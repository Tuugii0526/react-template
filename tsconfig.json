{
	// Solution-style config: references the per-folder configs. A reference path
	// that points at a directory resolves to <dir>/tsconfig.json.
	"files": [],
	"references": [{ "path": "./frontend" }, { "path": "./backend" }]
}
